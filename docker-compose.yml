services:
  app:
    image: ghcr.io/nisal-portcitybpo/meta-account-manager:latest
    container_name: meta-account-manager
    restart: unless-stopped
    ports:
      - "4000:4000"
    env_file:
      - .env.meta
    networks:
      - shared
networks:
  shared:
    external: true