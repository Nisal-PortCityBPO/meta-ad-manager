name: Build, Push to GHCR and Deploy

on:
  workflow_dispatch:
  push:
    branches:
      - dev

permissions:
  contents: write
  packages: write

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.bump.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Compute next 1.x.x version
        id: bump
        run: |
          git fetch --tags --force
          LATEST=$(git tag -l 'v1.*.*' | sort -V | tail -n1)
          if [ -z "$LATEST" ]; then
            NEW="1.0.0"
          else
            IFS='.' read -r MAJOR MINOR PATCH <<< "${LATEST#v}"
            NEW="${MAJOR}.${MINOR}.$((PATCH+1))"
          fi
          echo "version=${NEW}" >> "$GITHUB_OUTPUT"
          echo "Next version: ${NEW}"

      - name: Create and push tag
        env:
          VERSION: ${{ steps.bump.outputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag "v${VERSION}"
          git push origin "v${VERSION}"

  build:
    runs-on: ubuntu-latest
    needs: version
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Normalize image name
        id: image
        run: echo "image=ghcr.io/${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUBTOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ steps.image.outputs.image }}:${{ needs.version.outputs.version }}
            ${{ steps.image.outputs.image }}:latest

  deploy:
    runs-on: ubuntu-latest
    needs: [version, build]
    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        env:
          VERSION: ${{ needs.version.outputs.version }}
          GHCR_USER: ${{ github.actor }}
          GHCR_TOKEN: ${{ secrets.GITHUBTOKEN }}
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          envs: VERSION,GHCR_USER,GHCR_TOKEN
          script: |
            cd /var/www
            ./deploy_meta_app.sh "${VERSION}"


# name: Production CI/CD Release Pipeline

# on:
#   workflow_dispatch:
#   push:
#     branches:
#       - main

# concurrency:
#   group: production-deploy
#   cancel-in-progress: false

# permissions:
#   contents: write
#   packages: write

# jobs:
#   validate:
#     name: Quality Gate
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout source
#         uses: actions/checkout@v4

#       - name: Configure Node.js runtime
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm
#           cache-dependency-path: |
#             backend/package-lock.json
#             frontend/package-lock.json

#       - name: Install API dependencies
#         run: npm ci --prefix backend

#       - name: Install web dependencies
#         run: npm ci --prefix frontend

#       - name: Run API syntax gate
#         run: find backend/src -name '*.js' -print0 | xargs -0 -n1 node --check

#       - name: Build web artifact
#         run: npm run build --prefix frontend

#   version:
#     name: Release Version
#     runs-on: ubuntu-latest
#     needs: validate
#     outputs:
#       version: ${{ steps.bump.outputs.version }}
#     steps:
#       - name: Checkout release history
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0

#       - name: Resolve next semantic version
#         id: bump
#         run: |
#           git fetch --tags --force
#           LATEST=$(git tag -l 'v1.*.*' | sort -V | tail -n1)
#           if [ -z "$LATEST" ]; then
#             NEW="1.0.0"
#           else
#             IFS='.' read -r MAJOR MINOR PATCH <<< "${LATEST#v}"
#             NEW="${MAJOR}.${MINOR}.$((PATCH+1))"
#           fi
#           echo "version=${NEW}" >> "$GITHUB_OUTPUT"
#           echo "Next version: ${NEW}"

#   build:
#     name: Build and Publish Container Artifact
#     runs-on: ubuntu-latest
#     needs: version
#     steps:
#       - name: Checkout source
#         uses: actions/checkout@v4

#       - name: Resolve container image name
#         id: image
#         run: echo "image=ghcr.io/${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Authenticate to container registry
#         uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GHCR_PAT || secrets.GITHUBTOKEN || github.token }}

#       - name: Build and publish release artifact
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: ./Dockerfile
#           push: true
#           tags: |
#             ${{ steps.image.outputs.image }}:${{ needs.version.outputs.version }}
#             ${{ steps.image.outputs.image }}:latest

#   tag:
#     name: Release Tag
#     runs-on: ubuntu-latest
#     needs: [version, build]
#     steps:
#       - name: Checkout release history
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0

#       - name: Create immutable release tag
#         env:
#           VERSION: ${{ needs.version.outputs.version }}
#         run: |
#           git config user.name "github-actions[bot]"
#           git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
#           git tag "v${VERSION}"
#           git push origin "v${VERSION}"

#   deploy:
#     name: Production Deployment - ${{ matrix.name }}
#     runs-on: ubuntu-latest
#     needs: [version, build, tag]
#     strategy:
#       fail-fast: false
#       matrix:
#         include:
#           - name: cr_vps
#             host_secret: CR_VPS_HOST
#             user_secret: CR_VPS_USER
#             key_secret: CR_VPS_SSH_KEY
#             password_secret: CR_VPS_PASSWORD
#             port_secret: CR_VPS_SSH_PORT
#             app_dir_secret: CR_VPS_APP_DIR
#             app_port_secret: CR_VPS_APP_PORT
#             network_secret: CR_VPS_DOCKER_NETWORK
#           - name: ct_vps
#             host_secret: CT_VPS_HOST
#             user_secret: CT_VPS_USER
#             key_secret: CT_VPS_SSH_KEY
#             password_secret: CT_VPS_PASSWORD
#             port_secret: CT_VPS_SSH_PORT
#             app_dir_secret: CT_VPS_APP_DIR
#             app_port_secret: CT_VPS_APP_PORT
#             network_secret: CT_VPS_DOCKER_NETWORK
#           - name: v_vps
#             host_secret: V_VPS_HOST
#             user_secret: V_VPS_USER
#             key_secret: V_VPS_SSH_KEY
#             password_secret: V_VPS_PASSWORD
#             port_secret: V_VPS_SSH_PORT
#             app_dir_secret: V_VPS_APP_DIR
#             app_port_secret: V_VPS_APP_PORT
#             network_secret: V_VPS_DOCKER_NETWORK
#     steps:
#       - name: Checkout deployment automation
#         uses: actions/checkout@v4

#       - name: Resolve container image name
#         id: image
#         run: echo "image=ghcr.io/${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"

#       - name: Deploy release artifact over SSH
#         uses: appleboy/ssh-action@v1.2.0
#         env:
#           VERSION: ${{ needs.version.outputs.version }}
#           IMAGE: ${{ steps.image.outputs.image }}
#           GHCR_USER: ${{ github.actor }}
#           GHCR_TOKEN: ${{ secrets.GHCR_PAT || secrets.GITHUBTOKEN || github.token }}
#           APP_DIR: ${{ secrets[matrix.app_dir_secret] || secrets.VPS_APP_DIR }}
#           APP_PORT: ${{ secrets[matrix.app_port_secret] || secrets.VPS_APP_PORT }}
#           DOCKER_NETWORK: ${{ secrets[matrix.network_secret] || secrets.VPS_DOCKER_NETWORK }}
#         with:
#           host: ${{ secrets[matrix.host_secret] }}
#           username: ${{ secrets[matrix.user_secret] || secrets.VPS_SSH_USER }}
#           key: ${{ secrets[matrix.key_secret] || secrets.VPS_SSH_KEY }}
#           password: ${{ secrets[matrix.password_secret] || secrets.VPS_SSH_PASSWORD }}
#           port: ${{ secrets[matrix.port_secret] || secrets.VPS_SSH_PORT || '22' }}
#           envs: VERSION,IMAGE,GHCR_USER,GHCR_TOKEN,APP_DIR,APP_PORT,DOCKER_NETWORK
#           script_stop: true
#           script_path: deploy.sh

